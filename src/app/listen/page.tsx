import { Metadata } from 'next';

export const metadata:Metadata = {
  title: 'Music | Live Stream Here',
  description: 'sport streaming site in Nigeria',
}

export default function listen() {
  return (
    <div>
      listen
    </div>
  )
}
